You are given an image and a question about it. Look carefully and reason step by step.

If a detail is too small to read confidently — tiny labels, axis ticks, closely-packed or overlapping objects — use the `zoom` tool to magnify the region you care about before answering. The box is `[x1, y1, x2, y2]` given as fractions of the FULL image in `[0, 1]` (x1, y1 = top-left corner; x2, y2 = bottom-right corner, with x2 > x1 and y2 > y1); for example `[0.4, 0.4, 0.6, 0.6]` is a small patch in the centre. Coordinates always refer to the ORIGINAL full image, even after you have zoomed. You may zoom up to {max_crops} times, into different regions, to verify your reasoning.

When you are confident, stop zooming and give your final answer within \boxed{{}} (for example \boxed{{7}}).

Question: {question}
